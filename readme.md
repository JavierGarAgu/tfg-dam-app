# Django App README

1. This is a Django-based web application for managing tasks and users.
2. Requires Python 3.10+ and Django 4.0+.
3. Install dependencies with: pip install -r requirements.txt
4. Create a .env file for environment variables (DEBUG, DATABASE_URL, etc.).
5. Run database migrations: python manage.py migrate
